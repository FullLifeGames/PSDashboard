# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Pages deploy staging: the nightly plus the unpacked release builds
site
.versions-tmp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude
Gen9Draft-*
test-results
.playwright-mcp
FIXPLAN.md
docs

# Fitting corpus cache (manifest is committed; logs are re-downloadable)
.fit-corpus/

# Pinned Smogon payloads for the calibration harness's fills lever
.smogon-cache/

FeedbackEval.xlsx
deploy.ps1

# TypeScript incremental build state (composite packages write theirs into dist/)
*.tsbuildinfo

# Calibration slice runs (scripts/run-calibration.mjs): dumps, logs, summaries
.calibration/
